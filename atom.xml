<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zephoon.github.io</id>
    <title>zephoon</title>
    <updated>2020-02-10T03:58:30.100Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zephoon.github.io"/>
    <link rel="self" href="https://zephoon.github.io/atom.xml"/>
    <subtitle>code for life</subtitle>
    <logo>https://zephoon.github.io/images/avatar.png</logo>
    <icon>https://zephoon.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, zephoon</rights>
    <entry>
        <title type="html"><![CDATA[get_current_path]]></title>
        <id>https://zephoon.github.io/post/get_current_path</id>
        <link href="https://zephoon.github.io/post/get_current_path">
        </link>
        <updated>2020-02-10T03:47:59.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="get_current_path">get_current_path</h2>
<p>Difference between the <code>'.'</code> and the <code>__file__</code>.</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="get_current_path">get_current_path</h2>
<p>Difference between the <code>'.'</code> and the <code>__file__</code>.</p>
<!-- more -->
<ol>
<li>get the current <strong>execute path</strong> no matter where the code in any python file is</li>
</ol>
<pre><code class="language-python">from pathlib import Path
CURRENT_FILE_PATH = Path('.')
</code></pre>
<ol start="2">
<li>get the current <strong>python file path</strong> no matter where the execute path is</li>
</ol>
<pre><code class="language-python">from pathlib import Path
CURRENT_FILE_PATH = Path(__file__)
</code></pre>
<p>So the <code>__file__</code> is more safe to use in the python project.😃😃😃</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[pyspark 常用 api]]></title>
        <id>https://zephoon.github.io/post/pyspark-chang-yong-api</id>
        <link href="https://zephoon.github.io/post/pyspark-chang-yong-api">
        </link>
        <updated>2020-01-30T13:29:59.000Z</updated>
        <summary type="html"><![CDATA[<p>最近在做对接数据仓库的数据清洗和预处理，攒了一些pyspark常用api备用。😜😜😜</p>
]]></summary>
        <content type="html"><![CDATA[<p>最近在做对接数据仓库的数据清洗和预处理，攒了一些pyspark常用api备用。😜😜😜</p>
<!-- more -->
<h2 id="1-introduction-to-big-data-analysis-with-spark">1. Introduction to Big Data analysis with Spark</h2>
<h3 id="11-understanding-sparkcontext">1.1 Understanding SparkContext</h3>
<pre><code># Print the version of SparkContext
print(&quot;The version of Spark Context in the PySpark shell is&quot;, sc.version)

# Print the Python version of SparkContext
print(&quot;The Python version of Spark Context in the PySpark shell is&quot;, sc.pythonVer)

# Print the master of SparkContext
print(&quot;The master of Spark Context in the PySpark shell is&quot;, sc.master)
</code></pre>
<h3 id="12-interactive-use-of-pyspark">1.2 Interactive Use of PySpark</h3>
<pre><code># Create a python list of numbers from 1 to 100 
numb = range(1, 100)

# Load the list into PySpark  
spark_data = sc.parallelize(numb)
</code></pre>
<h3 id="13-loading-data-in-pyspark-shell">1.3 Loading data in PySpark shell</h3>
<pre><code># Load a local file into PySpark shell
lines = sc.textFile(file_path)
</code></pre>
<h3 id="14-use-of-lambda-with-map">1.4 Use of lambda() with map()</h3>
<pre><code># Print my_list in the console
print(&quot;Input list is&quot;, my_list)

# Square all numbers in my_list
squared_list_lambda = list(map(lambda x: x ** 2, my_list))

# Print the result of the map function
print(&quot;The squared numbers are&quot;, squared_list_lambda)
</code></pre>
<h3 id="15-use-of-lambda-with-filter">1.5 Use of lambda() with filter()</h3>
<pre><code># Print my_list2 in the console
print(&quot;Input list is:&quot;, my_list2)

# Filter numbers divisible by 10
filtered_list = list(filter(lambda x: (x%10 == 0), my_list2))

# Print the numbers divisible by 10
print(&quot;Numbers divisible by 10 are:&quot;, filtered_list)
</code></pre>
<h2 id="2-programming-in-pyspark-rdds">2. Programming in PySpark RDD’s</h2>
<h3 id="21-rdds-from-parallelized-collections">2.1 RDDs from Parallelized collections</h3>
<pre><code># Create an RDD from a list of words
RDD = sc.parallelize([&quot;Spark&quot;, &quot;is&quot;, &quot;a&quot;, &quot;framework&quot;, &quot;for&quot;, &quot;Big Data processing&quot;])

# Print out the type of the created object
print(&quot;The type of RDD is&quot;, type(RDD))
</code></pre>
<h3 id="22-rdds-from-external-datasets">2.2 RDDs from External Datasets</h3>
<pre><code># Print the file_path
print(&quot;The file_path is&quot;, file_path)

# Create a fileRDD from file_path
fileRDD = sc.textFile(file_path)

# Check the type of fileRDD
print(&quot;The file type of fileRDD is&quot;, type(fileRDD))
</code></pre>
<h3 id="23-partitions-in-your-data">2.3 Partitions in your data</h3>
<pre><code># Check the number of partitions in fileRDD
print(&quot;Number of partitions in fileRDD is&quot;, fileRDD.getNumPartitions())

# Create a fileRDD_part from file_path with 5 partitions
fileRDD_part = sc.textFile(file_path, minPartitions = 5)

# Check the number of partitions in fileRDD_part
print(&quot;Number of partitions in fileRDD_part is&quot;, fileRDD_part.getNumPartitions())
</code></pre>
<h3 id="24-map-and-collect">2.4 Map and Collect</h3>
<pre><code># Create map() transformation to cube numbers
cubedRDD = numbRDD.map(lambda x: x ** 3)

# Collect the results
numbers_all = cubedRDD.collect()

# Print the numbers from numbers_all
for numb in numbers_all:
	print(numb)
</code></pre>
<h3 id="25-filter-and-count">2.5 Filter and Count</h3>
<pre><code># Filter the fileRDD to select lines with Spark keyword
fileRDD_filter = fileRDD.filter(lambda line: 'Spark' in line.split(' '))

# How many lines are there in fileRDD?
print(&quot;The total number of lines with the keyword Spark is&quot;, fileRDD_filter.count())

# Print the first four lines of fileRDD
for line in fileRDD_filter.take(4): 
  print(line)
</code></pre>
<h3 id="26-reducebykey-and-collect">2.6  ReduceBykey and Collect</h3>
<pre><code># Create PairRDD Rdd with key value pairs
Rdd = sc.parallelize([(1,2),(3,4),(3,6),(4,5)])

# Apply reduceByKey() operation on Rdd
Rdd_Reduced = Rdd.reduceByKey(lambda x, y: x + y)

# Iterate over the result and print the output
for num in Rdd_Reduced.collect(): 
  print(&quot;Key {} has {} Counts&quot;.format(num[0], num[1]))
</code></pre>
<h3 id="27-sortbykey-and-collect">2.7 SortByKey and Collect</h3>
<pre><code># Sort the reduced RDD with the key by descending order
Rdd_Reduced_Sort = Rdd_Reduced.sortByKey(ascending=False)

# Iterate over the result and print the output
for num in Rdd_Reduced_Sort.collect():
  print(&quot;Key {} has {} Counts&quot;.format(num[0], num[1]))
</code></pre>
<h3 id="28-countingbykeys">2.8 CountingBykeys</h3>
<pre><code># Transform the rdd with countByKey()
total = Rdd.countByKey()

# What is the type of total?
print(&quot;The type of total is&quot;, type(total))

# Iterate over the total and print the output
for k, v in total.items(): 
  print(&quot;key&quot;, k, &quot;has&quot;, v, &quot;counts&quot;)
</code></pre>
<h3 id="29-create-a-base-rdd-and-transform-it">2.9 Create a base RDD and transform it</h3>
<pre><code># Create a baseRDD from the file path
baseRDD = sc.textFile(file_path)

# Split the lines of baseRDD into words
splitRDD = baseRDD.flatMap(lambda x: x.split())

# Count the total number of words
print(&quot;Total number of words in splitRDD:&quot;, splitRDD.count())
</code></pre>
<h3 id="210-remove-stop-words-and-reduce-the-dataset">2.10 Remove stop words and reduce the dataset</h3>
<pre><code># Convert the words in lower case and remove stop words from stop_words
splitRDD_no_stop = splitRDD.filter(lambda x: x.lower() not in stop_words)

# Create a tuple of the word and 1 
splitRDD_no_stop_words = splitRDD_no_stop.map(lambda w: (w, 1))

# Count of the number of occurences of each word
resultRDD = splitRDD_no_stop_words.reduceByKey(lambda x, y: x + y)
</code></pre>
<h3 id="211-print-word-frequencies">2.11 Print word frequencies</h3>
<pre><code># Display the first 10 words and their frequencies
for word in resultRDD.take(10):
	print(word)

# Swap the keys and values 
resultRDD_swap = resultRDD.map(lambda x: (x[1], x[0]))

# Sort the keys in descending order
resultRDD_swap_sort = resultRDD_swap.sortByKey(ascending=False)

# Show the top 10 most frequent words and their frequencies
for word in resultRDD_swap_sort.take(10):
	print(&quot;{} has {} counts&quot;. format(word[1], word[0]))
</code></pre>
<h2 id="3-pyspark-sql-dataframes">3. PySpark SQL &amp; DataFrames</h2>
<h3 id="31-rdd-to-dataframe">3.1 RDD to Dataframe</h3>
<pre><code># Create a list of tuples
sample_list = [('Mona', 20), ('Jennifer',34), ('John',20), ('Jim',26)]

# Create a RDD from the list
rdd = sc.parallelize(sample_list)

# Create a PySpark DataFrame
names_df = spark.createDataFrame(rdd, schema=['Name', 'Age'])

# Check the type of names_df
print(&quot;The type of names_df is&quot;, type(names_df))
</code></pre>
<h3 id="32-laoding-csv-into-dataframe">3.2 Laoding CSV into Dataframe</h3>
<pre><code># Create an DataFrame from file_path
people_df = spark.read.csv(file_path, header=True, inferSchema=True)

# Check the type of people_df
print(&quot;The type of people_df is&quot;, type(people_df))
</code></pre>
<h3 id="33-inspecting-data-in-pyspark-dataframe">3.3 Inspecting data in PySpark DataFrame</h3>
<pre><code># Print the first 10 observations 
people_df.show(10)

# Count the number of rows 
print(&quot;There are {} rows in the people_df DataFrame.&quot;.format(people_df.count()))

# Count the number of columns and their names
print(&quot;There are {} columns in the people_df DataFrame and their names are {}&quot;.format(len(people_df.columns), people_df.columns))
</code></pre>
<h3 id="34-pyspark-dataframe-subsetting-and-cleaning">3.4 PySpark DataFrame subsetting and cleaning</h3>
<pre><code># Select name, sex and date of birth columns
people_df_sub = people_df.select('name', 'sex', 'date of birth')

# Print the first 10 observations from people_df_sub
people_df_sub.show(10)

# Remove duplicate entries from people_df_sub
people_df_sub_nodup = people_df_sub.dropDuplicates()

# Count the number of rows
print(&quot;There were {} rows before removing duplicates, and {} rows after removing duplicates&quot;.format(people_df_sub.count(), people_df_sub_nodup.count()))
</code></pre>
<h3 id="35-filtering-your-dataframe">3.5 Filtering your DataFrame</h3>
<pre><code># Filter people_df to select females 
people_df_female = people_df.filter(people_df.sex == &quot;female&quot;)

# Filter people_df to select males
people_df_male = people_df.filter(people_df.sex == &quot;male&quot;)

# Count the number of rows 
print(&quot;There are {} rows in the people_df_female DataFrame and {} rows in the people_df_male DataFrame&quot;.format(people_df_female.count(), people_df_male.count()))
</code></pre>
<h3 id="36-running-sql-queries-programmatically">3.6 Running SQL Queries Programmatically</h3>
<pre><code># Create a temporary table &quot;people&quot;
people_df.createOrReplaceTempView(&quot;people&quot;)

# Construct a query to select the names of the people from the temporary table &quot;people&quot;
query = '''SELECT name FROM people'''

# Assign the result of Spark's query to people_df_names
people_df_names = spark.sql(query)

# Print the top 10 names of the people
people_df_names.show(10)
</code></pre>
<h3 id="37-sql-queries-for-filtering-table">3.7 SQL queries for filtering Table</h3>
<pre><code># Filter the people table to select female sex 
people_female_df = spark.sql('SELECT * FROM people WHERE sex==&quot;female&quot;')

# Filter the people table DataFrame to select male sex
people_male_df = spark.sql('SELECT * FROM people WHERE sex==&quot;male&quot;')

# Count the number of rows in both DataFrames
print(&quot;There are {} rows in the people_female_df and {} rows in the people_male_df DataFrames&quot;.format(people_female_df.count(), people_male_df.count()))
</code></pre>
<h3 id="38-pyspark-dataframe-visualization">3.8 PySpark DataFrame visualization</h3>
<pre><code># Check the column names of names_df
print(&quot;The column names of names_df are&quot;, names_df.columns)

# Convert to Pandas DataFrame  
df_pandas = names_df.toPandas()

# Create a horizontal bar plot
df_pandas.plot(kind='barh', x='Name', y='Age', colormap='winter_r')
plt.show()
</code></pre>
<h3 id="exercise">Exercise</h3>
<ol>
<li>Part 1: Create a DataFrame from CSV file</li>
</ol>
<pre><code># Load the Dataframe
fifa_df = spark.read.csv(file_path, header=True, inferSchema=True)

# Check the schema of columns
fifa_df.printSchema()

# Show the first 10 observations
fifa_df.show(10)

# Print the total number of rows
print(&quot;There are {} rows in the fifa_df DataFrame&quot;.format(fifa_df.count()))
</code></pre>
<ol start="2">
<li>Part 2: SQL Queries on DataFrame</li>
</ol>
<pre><code># Create a temporary view of fifa_df
fifa_df.createOrReplaceTempView('fifa_df_table')

print(fifa_df.printSchema())

# Construct the &quot;query&quot;
query = '''SELECT Age FROM fifa_df_table WHERE Nationality == &quot;Germany&quot;'''

# Apply the SQL &quot;query&quot;
fifa_df_germany_age = spark.sql(query)

# Generate basic statistics
fifa_df_germany_age.describe().show()
</code></pre>
<ol start="3">
<li>Part 3: Data visualization</li>
</ol>
<pre><code># Convert fifa_df to fifa_df_germany_age_pandas DataFrame
fifa_df_germany_age_pandas = fifa_df_germany_age.toPandas()

# Plot the 'Age' density of Germany Players
fifa_df_germany_age_pandas.plot(kind='density')
plt.show()
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[window配置pyspark]]></title>
        <id>https://zephoon.github.io/post/window-pei-zhi-pyspark</id>
        <link href="https://zephoon.github.io/post/window-pei-zhi-pyspark">
        </link>
        <updated>2020-01-19T13:37:13.000Z</updated>
        <content type="html"><![CDATA[<p>新公司配了一台Thinkpad，工作不能用自己使用多年的MacbookPro， 很多使用习惯从头再来，相当折腾。😭😭😭</p>
<hr>
<ol start="0">
<li>基本环境信息</li>
</ol>
<pre><code>window 10
python 3.7.6
conda 3.8.1
</code></pre>
<ol>
<li>下载</li>
</ol>
<p>安装方式：直接解压到对应的路径下面，例如D盘的根目录下。</p>
<p><a href="https://www.apache.org/dyn/closer.lua/spark/spark-2.4.4/spark-2.4.4-bin-hadoop2.7.tgz">apache spark</a></p>
<p><a href="https://github.com/cdarlint/winutils">winutils</a></p>
<ol start="2">
<li>配置环境变量</li>
</ol>
<pre><code>SPARK_HOME: C:/spark/spark-2.4.4-bin-hadoop2.7
HADOOP_HOME: C:/hadoop/winutils/hadoop-2.7.0
</code></pre>
<ol start="3">
<li>安装pyspark<br>
可以自行设置清华anaconda镜像源</li>
</ol>
<pre><code>conda install pyspark 
</code></pre>
<ol start="4">
<li>代码测试</li>
</ol>
<pre><code>import sys
import os
from pathlib import WindowsPath

SPARK_HOME_PATH = &quot;C:/spark/spark-2.4.4-bin-hadoop2.7&quot;
HADOOP_HOME_PATH = &quot;C:/hadoop/winutils/hadoop-2.7.0&quot;
os.environ['SPARK_HOME'] = SPARK_HOME_PATH
os.environ['HADOOP_HOME'] = HADOOP_HOME_PATH
if not SPARK_HOME_PATH in sys.path:
    sys.path.append(SPARK_HOME_PATH)
if not HADOOP_HOME_PATH in sys.path:
    sys.path.append(HADOOP_HOME_PATH)

import findspark 
findspark.init() 
import pyspark # only run after findspark.init() 
from pyspark.sql import SparkSession 
spark = SparkSession.builder.getOrCreate()  
df = spark.sql('''select 'spark' as hello ''') 
df.show()
</code></pre>
<ol start="5">
<li>done!!!😎😎😎</li>
</ol>
<hr>
<ol>
<li><a href="https://changhsinlee.com/install-pyspark-windows-jupyter/">install-pyspark-windows-jupyter</a></li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[V2rayX in macOS]]></title>
        <id>https://zephoon.github.io/post/v2rayx-in-macos</id>
        <link href="https://zephoon.github.io/post/v2rayx-in-macos">
        </link>
        <updated>2019-11-24T14:04:32.000Z</updated>
        <content type="html"><![CDATA[<p>ss和ssr经常被封😷😷😷，最终只能多花点时间实施v2ray方案了😎😎😎。</p>
<h2 id="环境">环境</h2>
<ul>
<li>macOS: 10.15.1</li>
<li>V2rayX:  1.5.1</li>
</ul>
<h2 id="配置信息">配置信息</h2>
<ul>
<li>V2ray订阅链接（自行搭建或者直接购买👻👻👻）</li>
<li>服务器分享vmess链接（自行搭建或者直接购买👻👻👻）</li>
</ul>
<h2 id="配置流程">配置流程</h2>
<ol>
<li>配置订阅链接
<ul>
<li>Configure... -&gt; Advanced -&gt; Subsription</li>
</ul>
</li>
<li>配置服务器分享vmess链接
<ul>
<li>Configure... -&gt; Import -&gt; Import from other links</li>
</ul>
</li>
<li>Server -&gt; Update subsription</li>
<li>一般选择【PAC Mode】</li>
<li>v2ray-core load 开启服务</li>
<li>查看view log<pre><code>2019/11/24 22:25:09 tcp:127.0.0.1:54781 accepted tcp:0.client-channel.google.com:443 
2019/11/24 22:25:09 tcp:127.0.0.1:54780 accepted tcp:safebrowsing.googleapis.com:443
</code></pre>
</li>
<li>OK! 😀😀😀</li>
</ol>
<p>😺😺😺good luck！</p>
<p>相关链接</p>
<ul>
<li><a href="https://www.v2ray.com">v2ray官方文档</a></li>
<li><a href="%5Bhttps://github.com/Cenmrev/V2RayX">v2rayX</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[安装fbprophet遇到libstdc++报错]]></title>
        <id>https://zephoon.github.io/post/an-zhuang-fbprophet-yu-dao-libstdcbao-cuo</id>
        <link href="https://zephoon.github.io/post/an-zhuang-fbprophet-yu-dao-libstdcbao-cuo">
        </link>
        <updated>2019-06-28T01:21:11.000Z</updated>
        <summary type="html"><![CDATA[<p><code>gcc</code>使用正常，但是就是没法正确安装fbprophet或者成功安装但没法运行。</p>
]]></summary>
        <content type="html"><![CDATA[<p><code>gcc</code>使用正常，但是就是没法正确安装fbprophet或者成功安装但没法运行。</p>
<!-- more --> 
<h3 id="环境">环境：</h3>
<ul>
<li>centos7.2</li>
<li>python3.6.7</li>
</ul>
<h3 id="报错信息">报错信息</h3>
<pre><code>pip install fbprophet
</code></pre>
<p>出现<code>gcc</code>报错。</p>
<pre><code>conda install -c anaconda fbprophet
</code></pre>
<p>安装过程没问题，但是在使用过程就出现报错：</p>
<pre><code>from fbprophet import Prophet
&gt;&gt;&gt; ImportError: /lib64/libstdc++.so.6: version 'GLIBCXX_3.4.21' not found
</code></pre>
<h3 id="解决处理">解决处理</h3>
<p>排查跟gcc版本有关，编译安装太花时间，scl安装还是报同样的错误。<br>
conda可以安装libgcc，</p>
<pre><code>conda install -c anaconda libgcc
</code></pre>
<p>安装之后：</p>
<pre><code>ls ~/anaconda3/lib | grep libstdc++
</code></pre>
<p>查询发现有对应的libstdc++.so.6.0.21。<br>
然后需要设置环境变量，</p>
<pre><code>export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:~/anaconda3/lib/libstdc++.so.6.0.21
</code></pre>
<p>执行：</p>
<pre><code>from fbprophet import Prophet
</code></pre>
<p>完美没报错！！！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[小批量PCA降维]]></title>
        <id>https://zephoon.github.io/post/xiao-pi-liang-pca-jiang-wei</id>
        <link href="https://zephoon.github.io/post/xiao-pi-liang-pca-jiang-wei">
        </link>
        <updated>2019-06-17T01:58:57.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="增量pca">增量PCA</h2>
<p>普通的PCA直接将全部训练集加载内存之中，这样内存很容易出现瓶颈。<br>
所以需要用到增量PCA。</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="增量pca">增量PCA</h2>
<p>普通的PCA直接将全部训练集加载内存之中，这样内存很容易出现瓶颈。<br>
所以需要用到增量PCA。</p>
<!-- more --> 
<h3 id="简单用法">简单用法</h3>
<pre><code>from sklearn.decomposition import IncrementalPCA

n_batches = 100
inc_pca = IncrementalPCA(n_components=54)
for X_batch in np.array_split(X_train, n_batches):
    inc_pca.partial_fit(X_batch)

X_reduced = inc_pca.transform(X_train)
</code></pre>
<h3 id="增量pca-npmemmap">增量PCA + np.memmap</h3>
<h4 id="numpymemmap简单说明">numpy.memmap简单说明</h4>
<pre><code>filename = &quot;yourself.data&quot;
m, n = X_train.shape

X_mm = np.memmap(filename, dtype='float32', mode='write', shape=(m, n))
X_mm[:] = X_train

del X_mm # 触发确保将数据缓存到disk
</code></pre>
<h4 id="配合numpymemmap使用按需加载数据">配合numpy.memmap使用，按需加载数据</h4>
<pre><code>X_mm = np.memmap(filename, dtype=&quot;float32&quot;, mode=&quot;readonly&quot;, shape=(m, n))

batch_size = m // n_batches
inc_pca = IncrementalPCA(n_components=154, batch_size=batch_size)
inc_pca.fit(X_mm)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[左移右移]]></title>
        <id>https://zephoon.github.io/post/zuo-yi-you-yi</id>
        <link href="https://zephoon.github.io/post/zuo-yi-you-yi">
        </link>
        <updated>2019-06-15T14:14:23.000Z</updated>
        <content type="html"><![CDATA[<pre><code>Z &gt;&gt; n
n &lt;&lt; Z
</code></pre>
<p>右移n位可以定义为除以<code>pow(2,n)</code>，左移n位可以定义为乘以<code>pow(2,n)</code>。但此结论只适用于该数左移时被溢出舍弃的高位中不包含1的情况。</p>
]]></content>
    </entry>
</feed>